import React from "react";

function TitleContent({ titleContent }) {
    return (
        <div className="title-content">
            <p>{titleContent}</p>
        </div>
    );
}

export default TitleContent;
